version: 1
backend:
  phases:
    build:
      commands:
        - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands: [npm run build]
  artifacts:
    discard-paths: yes
    baseDirectory: public
